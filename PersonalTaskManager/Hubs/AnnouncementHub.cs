using Microsoft.AspNetCore.SignalR;

namespace PersonalTaskManager.Hubs
{
    public class AnnouncementHub : Hub
    {
        // Optional: we can add server-side methods here if clients need to call
     }
}
